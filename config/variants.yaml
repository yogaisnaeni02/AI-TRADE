# Varian sistem — preset bernama untuk memilih dan membandingkan fitur.
#
# MASALAH YANG DISELESAIKAN
# -------------------------
# Sistem ini punya beberapa fitur yang masing-masing punya flag sendiri
# (confluence_filter, allow_sell, dst) tersebar di settings.yaml. Untuk
# menjalankan atau membandingkan satu kombinasi tertentu, sebelumnya harus
# mengedit settings.yaml manual — mudah salah, dan tidak ada cara cepat
# membandingkan "yang mana lebih baik" tanpa membaca kode.
#
# Tiap entri di sini adalah SATU KOMBINASI bernama. Semuanya override
# kecil di atas settings.yaml dasar — tidak menduplikasi seluruh config.
#
# CARA PAKAI
# ----------
#   python run_bot.py --varian konfluensi
#   python backtest/bandingkan_varian.py                 # semua varian
#   python backtest/bandingkan_varian.py --varian baseline konfluensi
#
# MAGIC NUMBER per varian supaya bisa jalan BERDAMPINGAN di akun yang sama
# tanpa bercampur di journal/dashboard. Basis 20260909 (sistem ini),
# +N per varian. JANGAN mengubah magic varian yang sudah pernah dipakai
# live — riwayat trade lama akan salah terhubung.
#
# Status "terverifikasi" merujuk ke dokumen yang mengukurnya. Sebelum
# memindahkan status apa pun ke "aktif produksi", baca dokumennya dulu.

# CATATAN PENTING: settings.yaml itu sendiri BUKAN netral. Sejak eksperimen
# dua-arah dimulai (11 Sep 2026), settings.yaml dasar sudah punya
# `momentum_fib.allow_sell: true` tertulis langsung di sana — bukan lewat
# varian. Supaya nama "baseline" benar-benar berarti "BUY saja, tanpa fitur
# tambahan" dan tidak diam-diam ikut hanyut saat settings.yaml berubah,
# baseline di sini SECARA EKSPLISIT me-reset setiap flag fitur ke nilai
# ilmiahnya (mati), bukan override: {}. Setiap varian lain juga menyatakan
# NILAI PENUH tiap flag yang relevan, bukan cuma yang diaktifkannya —
# supaya satu varian tidak diam-diam mewarisi flag varian lain lewat
# settings.yaml.

varian:

  baseline:
    magic: 20260909
    deskripsi: >
      BUY searah momentum, tanpa filter tambahan. Definisi ilmiah "polos" -
      dipertahankan tetap sama walau settings.yaml dasar berubah.
    status: forward-test-aktif
    override:
      momentum_fib:
        allow_sell: false
        confluence_filter: false

  konfluensi:
    magic: 20260910
    deskripsi: >
      Baseline + filter candle searah + ADX>25 + tren M5 tidak melawan.
      Satu-satunya kandidat yang lolos holdout tersegel DAN ambang t~3,0.
    status: terverifikasi-belum-produksi
    dokumen: docs/36-UJI-GABUNGAN-TEKNIK.md, docs/38-PERBANDINGAN-RISET-PARALEL.md
    hasil_backtest:
      n: 519
      E_R: 0.2510
      t: 3.14
      holdout_E_R: 0.2805
    override:
      momentum_fib:
        allow_sell: false
        confluence_filter: true

  dua_arah:
    magic: 20260911
    deskripsi: >
      Baseline + sisi SELL diaktifkan. Backtest bilang sell tidak punya
      edge (empat definisi rezim diuji, semua memperburuk) - dinyalakan
      sebagai eksperimen DEMO karena rezim turun saat ini jarang muncul
      di data historis. Ini VARIAN YANG SAMA dengan yang sedang berjalan
      lewat settings.yaml (allow_sell: true) - didaftarkan di sini supaya
      bisa dibandingkan lewat nama, bukan lewat membaca settings.yaml.
    status: eksperimen-demo-aktif
    dokumen: docs/43-KENAPA-CUMA-BUY-DAN-MOMEN-SELL.md
    peringatan: >
      JANGAN bawa ke akun real. Sisi sell belum lolos gerbangnya sendiri
      (docs/29 bagian 9).
    override:
      momentum_fib:
        allow_sell: true
        confluence_filter: false

  dua_arah_konfluensi:
    magic: 20260912
    deskripsi: >
      Gabungan dua_arah + konfluensi. BELUM diukur sebagai kombinasi -
      konfluensi diverifikasi hanya untuk BUY (docs/36). Menjalankan ini
      berarti menguji kombinasi baru, bukan mengulang hasil yang ada.
    status: belum-diukur
    override:
      momentum_fib:
        allow_sell: true
        confluence_filter: true

  pyramid5:
    magic: 20260914
    deskripsi: >
      Multi posisi SEARAH sampai 10 bersamaan, tanpa syarat jarak harga,
      dengan rentetan rugi dihitung PER BATCH bukan per posisi.

      Satu batch = sekelompok posisi yang terbuka bersamaan dari satu
      momentum. Batch berakhir saat SELURUH posisinya tutup. Batch dihitung
      rugi hanya bila SEMUA posisi di dalamnya rugi. Maks 3 batch rugi
      beruntun -> stop sisa hari.

      Permintaan pemilik: 10 posisi dari satu momentum adalah SATU taruhan,
      jadi tidak adil dihitung sebagai 10 loss terpisah.
    status: revisi-sl-tp-16sep2026
    dokumen: docs/52-MODE-BATCH.md
    peringatan: >
      DIREVISI TOTAL 16 Sep 2026 setelah forward test demo 15-16 Sep gagal
      (40 trade, winrate 15,0%, net -Rp 1.515.866). Yang diubah: SL/TP,
      waktu tahan, dan filter sinyal - lihat komentar per baris di bawah.

      Sinyal jalur tunggal setelah revisi (tanpa multi-posisi, 11.925
      sinyal dasar, spread + slippage dibayar):
        setelan LAMA : n=12.944  WR 24,8%  E[R] -0,0108  t -0,73
        setelan BARU : n= 2.693  WR 30,2%  E[R] +0,2117  t +5,98
      Holdout tersegel 30% yang tidak pernah dipakai memilih: t +2,85.

      YANG BELUM BERUBAH DAN MASIH BERISIKO: multi-posisi 10 bersamaan
      tanpa syarat jarak harga masih aktif, dan itu penyebab kejadian
      16 Sep (7 posisi BUY di rentang 2 dolar disapu serentak). Angka di
      atas TIDAK memodelkan penumpukan posisi - ia mengukur mutu sinyal
      per trade. Tetap eksperimen DEMO. JANGAN ke akun real.
    hasil_backtest:
      maxpos_1: {E_R: 0.0477, equity_akhir: 12731823}
      maxpos_2: {E_R: 0.0673, equity_akhir: 21304756}
      maxpos_3: {E_R: 0.0522, equity_akhir: 11714623}
      maxpos_5: {E_R: -0.1260, equity_akhir: -2373}
      maxpos_10: {E_R: -0.1332, equity_akhir: -140245}
    override:
      momentum_fib:
        allow_sell: true
        confluence_filter: false
        # Gate fibonacci dilonggarkan 0,75/0,25 -> 0,70/0,30 atas permintaan
        # pemilik, sama seperti autoclose_agresif.
        #
        # Bar bertren jelas yang lolos gate ini naik 34,5% -> 40,7% (+18%).
        #
        # DIUKUR pada pyramid5, dan angkanya MENIPU kalau dibaca sepintas:
        #   0,75/0,25 -> E[R] -0,1086  equity -Rp 332.935
        #   0,70/0,30 -> E[R] -0,0910  equity -Rp 162.344
        #
        # Terlihat membaik, TETAPI kedua-duanya kehabisan modal pertengahan
        # Agustus 2025 dan backtest berhenti di situ - tidak pernah mencapai
        # akhir data (Sep 2026). Yang terjadi bukan "lebih untung",
        # melainkan "modal habis sedikit lebih lambat".
        #
        # Pembanding yang sahih adalah baseline yang berjalan penuh, dan di
        # sana melonggarkan fib MEMPERBURUK (+0,1254 -> +0,1054).
        fib_buy_min: 0.70
        fib_sell_max: 0.30

        # ---------------------------------------------------------------
        # REVISI 16 Sep 2026 - STRATEGI SL/TP DIBEDAH ULANG
        #
        # Sebab: forward test demo 15-16 Sep menghasilkan 40 trade,
        # winrate 15,0%, net -Rp 1.515.866. Permintaan pemilik: perbaiki
        # pyramid5 langsung, "udah ku test hasilnya kurang bgt".
        #
        # Diagnosis dari log NYATA (bukan dugaan):
        #   RR terukur 2,60 - SUDAH sesuai target, TP tidak salah tempat
        #   winrate 15,0%  - breakeven butuh 28,0%
        # Jadi masalahnya kualitas sinyal, bukan penempatan TP.
        #
        # Dugaan "hampir TP lalu balik" DIUKUR pada 8.595 trade yang kena
        # SL: hanya 4,9% yang sampai >=80% jarak TP lalu berbalik; 59,9%
        # bahkan tidak mencapai seperempat jalan. Menggeser TP lebih dekat
        # TIDAK menolong - RR 1,5 s/d 2,5 semuanya terukur lebih buruk.
        #
        # confluence_filter dinyalakan dengan ambang ADX 35 (bukan 25).
        # Holdout tersegel 30%, tidak pernah dipakai memilih:
        #
        #   filter        DEV 70%            HOLDOUT 30%        WF
        #   mati (dulu)   E=-0,0031 t=-0,17  E=+0,0320 t=+1,14  3/5
        #   ADX>25        E=+0,0502 t=+1,68  E=+0,1042 t=+2,28  4/5
        #   ADX>35        E=+0,1281 t=+3,12  E=+0,1565 t=+2,51  4/5
        #
        # Konsekuensi: sinyal berkurang 58% (11.925 -> 4.956). Bot akan
        # jauh lebih sering diam. Itu memang tujuannya.
        confluence_filter: true
        confluence_adx_min: 35.0

      # SL/TP - pyramid5 satu-satunya varian yang menimpa ini.
      #
      # MASALAH: niat "SL = 1x ATR" tidak terwujud. Dengan jepitan
      # 3500-4500, dari 11.925 sinyal hanya 20,6% SL-nya benar-benar
      # ditentukan ATR - 79% menempel di batas:
      #   ATR < 3500 -> SL dipaksa 3500 : 29,7%
      #   ATR > 4500 -> SL dipaksa 4500 : 49,7%
      # Separuh waktu SL lebih sempit dari napas normal bar itu.
      #
      # Diukur (RR 2,78, 48 bar, seluruh sinyal):
      #   jepit 3500-4500  E[R] +0,0074  t +0,49  Rp  +7,3 jt
      #   jepit 3000-6000  E[R] +0,0187  t +1,23  Rp +15,1 jt
      #   SL bebas 1x ATR  E[R] +0,0317  t +2,06  Rp +26,8 jt
      #
      # SL bebas TIDAK dipakai meski angkanya terbaik: lot minimum 0,01
      # mengunci risiko, sehingga satu trade bisa rugi Rp 776.897 =
      # 19,42% equity. Batas 6000 menahan rugi maksimum di Rp 104.902
      # = 1,91% pada equity Rp 5,5 jt.
      trade_distances:
        sl_min_points: 3000       # dari 3500
        sl_max_points: 6000       # dari 4500 - perubahan terpenting
        sl_typical_points: 4500   # median ATR di sinyal ~4485
        # RR: puncak terukur di 3,0, bukan 2,78 (SL 3000-6000, 96 bar)
        #   RR 2,50  E[R] +0,0187  Rp +13,4 jt
        #   RR 2,78  E[R] +0,0187  Rp +15,1 jt
        #   RR 3,00  E[R] +0,0233  Rp +20,4 jt   <- dipilih
        #   RR 3,50  E[R] +0,0194  Rp +15,5 jt
        min_rr_ratio: 3.0
        tp_min_points: 9000       # = 3000 x 3,0, sejalan dengan sl_min

      position_management:
        # 48 -> 96 bar. Dari log NYATA pyramid5: trade yang MENANG butuh
        # median 92,8 menit, yang kalah hanya 29,1 menit. Batas 4 jam
        # memotong trade yang sedang berjalan benar.
        #
        # Diukur (SL 3000-6000, RR 2,78):
        #   48 bar ( 4 jam)  E[R] +0,0187  Rp +15,1 jt
        #   96 bar ( 8 jam)  E[R] +0,0270  Rp +21,7 jt   <- dipilih
        #  144 bar (12 jam)  E[R] +0,0266  Rp +21,7 jt   (datar)
        max_bars_hold: 96
        # breakeven_at_r 2,4 kini BENAR dengan sendirinya: pada RR 3,0
        # itu tepat 80% jarak TP, sesuai maksud asli komentar di
        # settings.yaml. Pada RR 2,78 nilainya diam-diam jadi 86,3%.
        breakeven_at_r: 2.4
    # SEMUA BATAS RUPIAH DINAIKKAN agar rem BATCH yang menyala duluan.
    #
    # Aritmetikanya (equity Rp 5.578.289, rugi per posisi Rp 78.676):
    #   1 batch (10 posisi) kena SL semua -> Rp   786.760 = 14,1%
    #   2 batch (20 posisi)               -> Rp 1.573.520 = 28,2%
    #   3 batch (30 posisi)               -> Rp 2.360.280 = 42,3%
    #
    # Supaya rem "3 batch beruntun" yang benar-benar menghentikan (bukan
    # rem rupiah yang menyala lebih dulu), batas rupiah harus di atas 42,3%.
    #
    # KONSEKUENSI YANG HARUS DISADARI: varian ini boleh kehilangan ~42%
    # modal dalam SATU HARI sebelum berhenti. Itu keputusan sadar pemilik
    # untuk eksperimen DEMO - bukan angka yang boleh dibawa ke akun real.
    override_risk:
      global:
        max_open_positions: 10
        # Circuit breaker dinaikkan 20% -> 50%. Tanpa ini, breaker menyala
        # di batch ke-2 dan MENGHENTIKAN SISTEM TOTAL (butuh reset manual),
        # sehingga rem batch tidak pernah sempat diuji.
        max_drawdown_percent: 50.0
      daily:
        loss_mode: batch
        max_consecutive_loss_batches: 3
        # 3 batch x 10 posisi = 30 trade. Batas lama 12 habis di batch
        # pertama, jadi konsep batch tidak akan pernah terpakai.
        #
        # DINAIKKAN 35 -> 100 (15 Sep 2026) atas permintaan pemilik untuk
        # dicoba langsung di demo. Diukur lebih dulu, dan efeknya nihil:
        #
        #   max_trades  35 : n=793  E[R]=-0,1030  equity akhir -Rp  24.350
        #   max_trades  50 : n=798  E[R]=-0,1086  equity akhir -Rp 332.935
        #   max_trades 100 : n=798  E[R]=-0,1086  equity akhir -Rp 332.935
        #
        # 50 dan 100 IDENTIK karena batas 50 saja tidak pernah tercapai.
        # Sebabnya dua-duanya terukur:
        #   1. sinyalnya memang tidak sebanyak itu - dari 87 hari ada trade,
        #      rata-rata 9,1/hari, maksimum yang pernah terjadi 43, dan
        #      NOL hari mencapai 50.
        #   2. saat rugi, batas rugi harian 45% tercapai di posisi ke-32,
        #      jadi batas 100 tidak akan pernah tersentuh.
        #
        # Naik dari 35 ke 50 justru MEMPERBURUK (equity -24rb -> -333rb):
        # 5 trade tambahan itu semuanya merugikan.
        max_trades: 100
        max_loss_percent: 45.0
      weekly:
        # Mingguan juga harus di atas 42,3%, kalau tidak rem mingguan yang
        # menyala duluan di hari pertama.
        max_loss_percent: 45.0

  autoclose:
    magic: 20260915
    deskripsi: >
      Baseline + auto-close saat skor keyakinan (0-100) turun >=20 poin
      dari nilai saat posisi dibuka, HANYA bila posisi sedang profit.
      Tujuannya mengunci sebagian keuntungan saat kondisi yang mendasari
      sinyal sudah memudar - bukan memotong kerugian (itu tugas SL).
    status: terukur-tipis-belum-produksi
    dokumen: docs/53-AUTOCLOSE-SKOR.md
    hasil_backtest:
      tanpa_autoclose: {E_R: 0.2815, t: 5.92}
      turun_10: {E_R: 0.2477}
      turun_15: {E_R: 0.2835}
      turun_20: {E_R: 0.2994, catatan: "terbaik"}
      turun_25: {E_R: 0.2855}
      turun_40: {E_R: 0.2672}
      stabilitas: "positif kedua paruh (+0,013 / +0,025)"
      kena: "237 dari 1440 trade (16,5%)"
    peringatan: >
      DIUKUR ULANG seperti bot live (backtest/simulasi_live.py, docs/70):
      auto-close KALAH dari induknya dua_arah - 100 hari data Exness +47,2R
      vs +60,6R, 30 hari -11,6R vs -0,5R. Ia lebih sering memotong trade
      yang akan TP daripada menyelamatkan yang akan SL. Angka +0,018R di
      bawah diukur tanpa memodelkan cara bot menilai skor, jadi tidak
      berlaku untuk bot live. Pertimbangkan menghentikan varian ini.
    override:
      momentum_fib:
        allow_sell: true
        confluence_filter: false
      position_management:
        autoclose_score_drop: 20
        # Skor dinilai di bar M5 yang SUDAH TUTUP, bukan bar yang sedang
        # terbentuk (candle dan momentum berkedip selama bar berjalan).
        # Diukur dengan backtest/simulasi_live.py, halt DD mati (docs/70):
        #                            bar berjalan  bar tutup  tanpa autoclose
        #   100 hari Exness (Jun-Sep)    +44,38R     +47,17R    +60,61R
        #   30 hari (16 Agu-15 Sep)      -15,66R     -11,61R     -0,53R
        # Bar tutup lebih baik di kedua periode, tetapi tetap kalah dari
        # tanpa auto-close - lihat peringatan.
        autoclose_bar: tutup

  autoclose_agresif:
    magic: 20260916
    deskripsi: >
      Gabungan pyramid5 + auto-close, atas permintaan pemilik: kerangka
      pyramiding penuh (10 posisi searah, mode batch, 100 trade/hari)
      DITAMBAH auto-close saat skor keyakinan turun >=15 poin.

      Idenya: pyramiding menangkap momentum selagi berlangsung, auto-close
      melepas posisi begitu momentum itu memudar - tanpa menunggu SL.
    status: eksperimen-demo-backtest-negatif
    dokumen: docs/53-AUTOCLOSE-SKOR.md
    peringatan: >
      KEDUA komponennya diukur lebih dulu dan KEDUANYA memperburuk:
      pyramiding 10 posisi membuat modal HABIS (E[R] -0,133), dan ambang
      auto-close 15 lebih buruk dari 20 (+0,2835 vs +0,2994).

      Kombinasinya BELUM diukur - auto-close mungkin meredam kerugian
      pyramiding, mungkin juga tidak. Itu justru yang diuji di demo.
      Sama seperti pyramid5: boleh kehilangan ~42% modal dalam sehari
      sebelum berhenti. JANGAN ke akun real.
    hasil_backtest:
      ambang_autoclose:
        turun_20: {E_R: 0.2994, catatan: "terbaik"}
        turun_15: {E_R: 0.2835, catatan: "dipakai varian ini - lebih agresif"}
        turun_10: {E_R: 0.2477}
      pyramiding:
        maxpos_2: {E_R: 0.0673, equity: 21304756, catatan: "terbaik"}
        maxpos_5: {E_R: -0.1260, equity: -2373}
        maxpos_10: {E_R: -0.1332, equity: -140245, catatan: "dipakai varian ini"}
      kombinasi: "BELUM DIUKUR - itu yang dicari lewat forward test demo"
    override:
      momentum_fib:
        allow_sell: true
        confluence_filter: false
        # Gate fibonacci dilonggarkan 0,75/0,25 -> 0,70/0,30 atas permintaan
        # pemilik, supaya sinyal lebih sering muncul.
        #
        # Gate ini penyaring TERBESAR: dari 86.309 bar bertren jelas, yang
        # lolos naik dari 34,5% (0,75) ke 40,7% (0,70) - +18% peluang.
        #
        # DIUKUR, dan hasilnya HARUS dibaca hati-hati:
        #   baseline           0,75/0,25 -> E[R] +0,1254  equity Rp 23,8 jt
        #   baseline           0,70/0,30 -> E[R] +0,1054  equity Rp 22,8 jt
        #   autoclose_agresif  0,75/0,25 -> E[R] -0,1086  equity MINUS
        #   autoclose_agresif  0,70/0,30 -> E[R] -0,0910  equity MINUS
        #
        # Di varian agresif angkanya terlihat MEMBAIK, tetapi itu MENIPU:
        # kedua-duanya kehabisan modal di pertengahan Agustus 2025 dan
        # backtest berhenti di situ - tidak pernah sampai akhir periode.
        # Yang terjadi bukan "lebih untung", melainkan "modal habis sedikit
        # lebih lambat".
        #
        # Angka yang bisa dipercaya datang dari baseline yang berjalan
        # penuh: di sana melonggarkan fib MEMPERBURUK (+0,1254 -> +0,1054).
        fib_buy_min: 0.70
        fib_sell_max: 0.30

        # ---------------------------------------------------------------
        # REVISI 16 Sep 2026 - STRATEGI SL/TP SAMA DENGAN pyramid5
        #
        # Permintaan pemilik: kedua varian agresif diperbaiki bersama.
        # Alasan dan seluruh pengukurannya ada di blok pyramid5 di atas -
        # tidak diulang di sini supaya satu sumber kebenaran.
        #
        # Ringkas: filter ADX 35 pada holdout tersegel 30% memberi
        # E=+0,1565 t=+2,51 (dulu mati: E=+0,0320 t=+1,14). Sinyal
        # berkurang 58%.
        confluence_filter: true
        confluence_adx_min: 35.0

      # SL/TP - sama persis dengan pyramid5. Lihat blok pyramid5 untuk
      # tabel pengukuran lengkapnya.
      #   jepit 3500-4500 (lama)  E[R] +0,0074  t +0,49  Rp  +7,3 jt
      #   jepit 3000-6000 (baru)  E[R] +0,0187  t +1,23  Rp +15,1 jt
      # SL bebas 1x ATR lebih bagus lagi tetapi TIDAK dipakai: lot minimum
      # mengunci risiko, satu trade bisa rugi 19,42% equity.
      trade_distances:
        sl_min_points: 3000       # dari 3500
        sl_max_points: 6000       # dari 4500 - perubahan terpenting
        sl_typical_points: 4500
        min_rr_ratio: 3.0         # puncak terukur, dari 2,78
        tp_min_points: 9000       # = 3000 x 3,0

      position_management:
        autoclose_score_drop: 15
        # Bar tutup, bukan bar berjalan (docs/70). Diukur pada SL/TP LAMA,
        # halt DD mati:
        #                     bar berjalan        bar tutup           pyramid5
        #   100 hari Exness   -16,27R (467 entry) -13,29R (170 entry) -12,23R
        #   30 hari           -22,85R (162 entry) -19,86R  (87 entry) -21,09R
        # Di bar berjalan, auto-close sering menutup dengan untung beberapa
        # ratus rupiah; satu posisi untung itu membuat batch "tidak rugi
        # semua", sehingga rem 3 batch rugi tidak pernah menyala dan entry
        # membengkak. Di bar tutup rem batch kembali bekerja.
        #
        # DIUKUR ULANG setelah revisi SL/TP 16 Sep (61c19ef), modal Rp 3 jt,
        # halt DD mati (docs/70 bagian 11). Angka revisi di commit itu
        # (+17,79R) SUDAH memakai bar tutup - direproduksi persis.
        #
        #                     bar berjalan              bar tutup (dipakai)
        #   100 hari Exness   +30,15R  maxDD 41,6%      +17,79R  maxDD 55,7%
        #   30 hari           -12,13R  modal habis      -10,02R  modal habis
        #                              sejak 21 Agu              sejak 15 Sep
        #
        # TIDAK KONSISTEN: di SL/TP lama bar tutup menang di kedua periode;
        # di SL/TP baru periode panjang memilih bar berjalan (+12R), periode
        # pendek memilih bar tutup (+2R, tercemar efek jalur - versi bar
        # berjalan kehabisan modal 25 hari lebih awal). Dibiarkan "tutup"
        # karena tidak ada dasar kuat untuk membalik. KEPUTUSAN TERBUKA.
        autoclose_bar: tutup
        # 48 -> 96 bar, sama dengan pyramid5. Dari log NYATA: trade yang
        # MENANG butuh median 92,8 menit, yang kalah hanya 29,1 menit.
        #
        # CATATAN KHUSUS varian ini: auto-close sudah memotong posisi saat
        # skor turun, jadi memperpanjang batas waktu TIDAK berarti posisi
        # digantung 8 jam - yang memudar tetap ditutup lebih dulu.
        max_bars_hold: 96
        # Pada RR 3,0 nilai 2,4 kembali berarti 80% jarak TP, sesuai
        # maksud aslinya. Pada RR 2,78 diam-diam jadi 86,3%.
        breakeven_at_r: 2.4
    override_risk:
      global:
        max_open_positions: 10
        # Sama seperti pyramid5: breaker dinaikkan supaya rem BATCH yang
        # menyala duluan, bukan breaker yang menghentikan sistem total.
        max_drawdown_percent: 50.0
      daily:
        loss_mode: batch
        max_consecutive_loss_batches: 3
        max_trades: 100
        max_loss_percent: 45.0
      weekly:
        max_loss_percent: 45.0

  # ---------------------------------------------------------------------
  # VARIAN UJI SEMENTARA - khusus akun DEMO, HAPUS setelah testing selesai
  #
  # Salinan persis empat varian di atas dengan SATU perbedaan: halt
  # drawdown global dimatikan (max_drawdown_percent 100). Tujuannya
  # mengumpulkan data tanpa bot berhenti permanen di tengah jalan. Batas 12
  # trade/hari, 3 loss beruntun, rugi harian 4% dan mingguan 8% TETAP aktif.
  #
  # Kenapa HANYA halt DD yang dimatikan (docs/70 bagian 10, 100 hari Exness):
  #   semua rem aktif     : keempat varian HALT di awal, cuma 23-77 entry
  #   halt DD dimatikan   : baseline +33,70R  konfluensi +32,61R
  #                         dua_arah +60,61R  dua_arah_konfluensi +50,11R
  #   batas trade dibuka  : LEBIH BURUK di dua periode - 57 trade tambahan,
  #                         semua masuk di hari yang sudah untung & panas
  #                         (>=3 TP), hanya 3 yang menang, total -45,9R
  #   tanpa batas apa pun : dua varian dua arah kehabisan modal
  #
  # Magic sendiri supaya journal dan sidik jari tidak tercampur dengan
  # varian aslinya. JANGAN ke akun real: tanpa halt, drawdown terukur ~24%.
  # ---------------------------------------------------------------------

  uji_baseline:
    magic: 20260917
    deskripsi: >
      baseline tanpa halt drawdown permanen - khusus uji demo.
    status: uji-demo-sementara
    dokumen: docs/70-SIMULASI-LIVE-DAN-PERBAIKAN-REM.md
    peringatan: >
      Halt drawdown 20% DIMATIKAN. Hanya akun demo; hapus setelah uji.
    override:
      momentum_fib:
        allow_sell: false
        confluence_filter: false
    override_risk:
      global:
        max_drawdown_percent: 100.0

  uji_konfluensi:
    magic: 20260918
    deskripsi: >
      konfluensi tanpa halt drawdown permanen - khusus uji demo.
    status: uji-demo-sementara
    dokumen: docs/70-SIMULASI-LIVE-DAN-PERBAIKAN-REM.md
    peringatan: >
      Halt drawdown 20% DIMATIKAN. Hanya akun demo; hapus setelah uji.
    override:
      momentum_fib:
        allow_sell: false
        confluence_filter: true
    override_risk:
      global:
        max_drawdown_percent: 100.0

  uji_dua_arah:
    magic: 20260919
    deskripsi: >
      dua_arah tanpa halt drawdown permanen - khusus uji demo.
    status: uji-demo-sementara
    dokumen: docs/70-SIMULASI-LIVE-DAN-PERBAIKAN-REM.md
    peringatan: >
      Halt drawdown 20% DIMATIKAN. Hanya akun demo; hapus setelah uji.
      Sisi sell tetap belum punya edge terbukti (docs/43).
    override:
      momentum_fib:
        allow_sell: true
        confluence_filter: false
    override_risk:
      global:
        max_drawdown_percent: 100.0

  uji_dua_arah_konfluensi:
    magic: 20260920
    deskripsi: >
      dua_arah_konfluensi tanpa halt drawdown permanen - khusus uji demo.
    status: uji-demo-sementara
    dokumen: docs/70-SIMULASI-LIVE-DAN-PERBAIKAN-REM.md
    peringatan: >
      Halt drawdown 20% DIMATIKAN. Hanya akun demo; hapus setelah uji.
    override:
      momentum_fib:
        allow_sell: true
        confluence_filter: true
    override_risk:
      global:
        max_drawdown_percent: 100.0

  ml_walkforward:
    magic: 20260913
    deskripsi: >
      Baseline + filter ML walk-forward (buang 25% probabilitas TP
      terendah). AUC 0,56 out-of-sample, lolos stabilitas seed dan sapuan
      hiperparameter (docs/46). BELUM diintegrasikan ke RuleEngine -
      masih di scripts/ml_walk_forward.py sebagai riset berdiri sendiri.
    status: riset-belum-diintegrasikan
    dokumen: docs/46-VERIFIKASI-ML-WALK-FORWARD.md
    override: {}   # tidak bisa diaktifkan lewat config - lihat status
